import { CurrentPostPipePipe } from './current-post-pipe.pipe';

describe('CurrentPostPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentPostPipePipe();
    expect(pipe).toBeTruthy();
  });
});
